# Opaque-to-glass transfer V3 targeted validity closure

V1 and V2 are preserved but `INVALID_REJECTED` with exact source/evidence commit
receipts. V3 uses calibration seeds 4301--4304, held-out seeds 7201--7212, and a new
material namespace. Before freeze, tests/calibration construct only absent/opaque.

Commands plus authenticated XML are the causal record. Scoring independently
rerenders initial RGB/depth/PNG, reconstructs controller identity/path/config,
detector, target and recovery decisions, replays commands in MuJoCo, and uses only
replayed state/contact for completion, safety, path and collision. Stored qpos/qvel,
contacts, detections and labels are audit receipts that must match but cannot create
an outcome. Seed-derived expected XML physics and XML-derived material/condition
reject coherent scene substitutions.

The exact root/raw/derived/manifest closure rejects extras and symlinks. Validation
rebuilds every score, CSV, bootstrap contrast, SVG, PNG graph, sample and report from
raw into a temporary root and byte-compares every derived artifact. Reconstruction
copies raw/closure only and performs the same rebuild. V3 runs a fresh frozen 108
episode matrix; no real-world glass or hierarchy-superiority authority follows.
