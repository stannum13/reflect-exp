# P5 Temporal Action-Chunk Execution Design

Date: 2026-08-22

Status: approved autonomous default. P5 requires complete P3 provenance and a completed, eligible P4 confirmation.

## Outcome and hard P4 eligibility

Experiment 02 compares seven ways to alter only unissued future samples on P4's simulated arm. It measures bounded latency/reactivity/smoothness trade-offs, not VLA intelligence, hardware asynchronous safety, or real-time-control equivalence.

P5 may execute only if P4's published promotion decision contains exact P2 or P4. Only those stack IDs have multi-knot trajectories. Frozen input records the promoted IDs and representations; the first promoted eligible ID is PRIMARY and a second is DESCRIPTIVE. If neither exists, P5 is prerequisite-blocked, produces no scientific label, and must not turn a one-knot stack into a trajectory.

That eligibility statement is a verified hash chain, not a field lookup. Before importing MuJoCo or creating any scientific/stage P5 output, the gate opens descriptor-relative, no-follow, stable snapshots of exactly `experiments/01_policy_control/configs/p3-gate.yaml`, `configs/frozen.yaml`, `protocol/frozen-config.sha256`, `protocol/confirmation/seed-manifest.json`, `protocol/confirmation/protocol-manifest.json`, `protocol/confirmation/preregistration-binding.json`, `results/confirmation/artifact-manifest.json`, `results/confirmation/decision.json`, and `protocol/confirmation/artifact-digests.json`. It uses duplicate-key-rejecting canonical loaders, rejects an unlisted sibling or changed inode/size/mtime/content on re-snapshot, and verifies every content/Git-blob/protocol/config/frozen/analyze/decision digest through the final report marker. The P3 gate is the authentic post-implementation P4 input created and committed by P4's pre-pilot revision: its canonical bytes and SHA-256 must equal every P4 protocol manifest's `p3_gate_sha256`, its embedded P2/P3 identities must match the independently validated P2/P3 evidence, and the preregistration and publication commits must contain the same Git blob and content bytes. The binding's `preregistration_git_sha` must be a real commit containing byte-identical seed and protocol manifests; the decision requires exact `lifecycle_state=COMPLETE`, `artifact_state=VALID`, a completed confirmation phase, and `promoted_stacks` containing P2 or P4 with matching stable-first-occurrence wire representations. The tracked P5 pre-pilot config declares the 40-hex P4 publication commit; that commit must be an ancestor of P5's implementation commit and contain byte-identical final P4 tracked evidence/report files. The current P5 worktree must be clean, but HEAD need not equal the older P4 publication commit.

The gate also reads P4's 40-hex `implementation_sha`, proves it is a commit, and requires byte equality to that commit for the immutable implementation allowlist: `Makefile,pyproject.toml,uv.lock,experiments/__init__.py,experiments/01_policy_control/__init__.py,experiments/01_policy_control/run.py,experiments/01_policy_control/src,experiments/01_policy_control/tests,experiments/01_policy_control/configs/base.yaml,experiments/01_policy_control/README.md,experiments/01_policy_control/CLAIM.md,experiments/01_policy_control/EXPERIMENT.md`. Relative to that implementation commit, later tracked changes are allowed only below the exact evidence roots `experiments/01_policy_control/protocol/pilot-r1/` and `experiments/01_policy_control/protocol/confirmation/`, at `configs/p3-gate.yaml`, `configs/frozen.yaml`, `protocol/frozen-config.sha256`, `RESULTS.md`, `INTERFACE_FINDINGS.md`, and the five P4 SVG paths listed by `artifact-digests.json`; their schemas and hash chains must account for every changed file. The P3 gate is the sole allowed post-implementation config addition other than frozen config and must satisfy the preceding cross-commit proof; it is never treated as implementation drift. No implementation/source path may differ, and no untracked file may exist under an implementation-owned path. P5 freezes the final P4 publication Git SHA, preregistration Git SHA, implementation SHA, canonical allowlist path/blob/content ledger hash, P3-gate Git-blob/content hashes, and every exact evidence digest. Any missing, duplicate, dirty, symlinked, nonregular, hash-mismatched, incomplete, invalid, unaccounted changed path, or post-read-mutated input blocks P5 with no scientific result.

P5 also freezes the shared P1 runtime it actually executes rather than trusting a clean later commit. The exact sorted module ledger covers `reflect/_rollout_io.py`, `reflect/clock.py`, `reflect/events.py`, `reflect/replay.py`, `reflect/rollout.py`, `reflect/safety.py`, and `reflect/types.py`; each row has exactly `path,git_blob_sha1,sha256`. `_rollout_io.py` is included because the imported `RolloutWriter`/validator delegates its descriptor-safe reads, writes, `fsync`, temporary-directory cleanup, and stable snapshots to that module; it is executable runtime, not an implementation detail outside the boundary. A physics-free P5-local conformance probe emits canonical bytes with exact keys `schema_version,action_representation_values,action_chunk_fields,control_reference_fields,execution_event_values,rollout_schema_version,rollout_lifecycle_fixture_hash,replay_fixture_hash,safety_fixture_hash,module_ledger_hash`; enum/field arrays must equal the approved P1 contracts, while the three fixture hashes cover canonical outputs from constructing/validating/replaying one closed lifecycle fixture and rejecting the closed unsafe-environment fixture through the existing imported APIs. It requires no new P1 constant or shared-code edit. The implementation commit must contain every ledger blob, and the byte-equal seven-row ledger plus conformance-probe hash is revalidated before and after every pilot shard/reduction, freeze, confirmation shard, analyze, and report invocation. A committed or worktree change to one of these seven modules is `SOURCE_DRIFT`; unrelated concurrent commits remain allowed. P5 does not copy, fork, or broaden a P1 abstraction.

A prerequisite failure is `NOT_RUN`, not an `INCONCLUSIVE` experiment. A non-dry live invocation that completes the safe gate far enough to identify a closed failure reason may create only ignored `experiments/02_action_chunks/results/preflight/status.json`; it creates no config, seed, bundle, analyze, or report artifact and never replaces the reviewed Markdown placeholders. The create-only canonical status has exact keys `schema_version,study_id,lifecycle_state,artifact_state,availability_status,scientific_result,promotion_state,runtime_authority,failed_gate,evidence_hashes,reasons`. Values are exactly `lifecycle_state=NOT_RUN`, `artifact_state=VALID`, `availability_status=FAILED_GATE`, `scientific_result=null`, `promotion_state=INCONCLUSIVE`, and `runtime_authority={deterministic_trajectory:NONE,flow_policy:NO_RUNTIME_AUTHORITY}`. `failed_gate` is one of `P2_INCOMPLETE|P3_INCOMPLETE|P4_INELIGIBLE|P4_EVIDENCE_INVALID|SOURCE_DRIFT|TREE_DIRTY`; `evidence_hashes` has the same exact keys as frozen evidence with unavailable values null; and `reasons` is a one-row array with exact keys `reason_id,detail`, `reason_id=PREREQUISITE_BLOCKED`, and the bounded safe detail rules below. The status bytes describe only gate availability, not experiment evidence. Exact reissue validates and skips; a conflict fails closed. `--dry-run` never writes it and prints the same closed object to stdout. `decision.json` and `ACTION_EXECUTION_DECISION.md` do not exist in this state; the autonomous top-level run report cites the status and literal program availability value `FAILED_GATE`.

P5 freezes its proposal rate before pilot at `P=0.100 s`, never outcome-selected. P4 has `D=0.002 s`, so `Q=P/D=50` ticks, `H=ceil(0.8/P)+1=9`, `E=ceil(2.5P/D)=125`, and `N_knot=(H-1)Q+1=401`. The usable proposal horizon is `N=min(401,125)=125`, indexed `0..124`, with 124 future ticks. Eligibility records the selected stack/representation, these exact values, raw expiry, one receding-prefix length `K_B` in `{1,2,4}`, and one asynchronous prefetch lead `J_E` in `{25,75,124}`. It proves `K_B+2<=124` and `1<=J_E<N`; the two quantities are never aliased merely because they occupy corresponding pilot-vector positions. Missing adapter/rate/horizon proof blocks P5 rather than relocating perturbations.

## Boundary, data flow, and virtual timing

The selected approach is the P4 task plus a project-local virtual-time broker. A pure-Python point-mass fixture is tests-only; LeRobot/OpenPI runtime adaptation is deferred. P5 adds no VLA, checkpoint, server, ROS, CUDA, remote transport, physical connection, copied upstream source, or shared Reflect schema change.

P4 arm/target -> `Observation+SkillSpec` -> raw proposal schedule -> broker -> executable `ActionChunk` -> unchanged P4 executor/controller -> MuJoCo -> `ControlReference`/events/metrics -> atomic evidence bundle.

`VirtualClock` is the only behavior-time source. At tick `t`, the deterministic order is: apply exogenous target moves; request phase (create at most one observation/request if capacity was free at the start of this phase); delivery phase ordered by `(delivery_tick, request_sequence)`; broker transition; execute P4; record telemetry; advance. A delivery at `t` therefore cannot free capacity for another request until `t+1`. Host time is measurement only. First issue copies the absolute-tick reference to a C-contiguous read-only array in append-only `issued[tick]` and `ControlReference`; later deliveries cannot mutate it, and tests hash all prior values after every delivery.

## Common setup and exogenous core schedule

Every A--G rollout begins with the same deterministic setup. At tick 0 the runner stores the initial observation and emits, in this exact `(monotonic_time_ns,sequence_id)` order, `OBSERVATION_RECEIVED(observation_id=o0)`, `POLICY_REQUESTED(source_observation_id=o0)`, `POLICY_RESPONDED(source_observation_id=o0,chunk_id=w0)`, and `CHUNK_ACCEPTED(chunk_id=w0)`. The selected P4 stack evaluates `o0` exactly once, outside the measured protocol, and its valid raw is the stored warm-up `ActionChunk w0`; every event uses the same skill, rollout, and config identities required by the canonical validator. Its issued reference must differ bytewise from the tick-0 reference on at least one tick in `1..52`, and every issued value must be finite and dimensionally valid. A setup is invalid only for a nonfinite value, dimension mismatch, reference outside the hard joint/workspace envelope before the common safety clamp, joint-limit escape, or unbounded controller divergence. Ordinary activation of P4's `1.5 rad/s` reference slew limiter and bounded torque saturation are retained safety metrics and do not invalidate warm-up. A bytewise-constant/hold setup invalidates the cell. Warm-up `ControlReference` and `ACTION_EXECUTED` rows are canonical rollout and safety/continuity evidence, but ticks before S and the setup response/request are excluded from measured idle fraction, request rate, proposal age, replacement, invalidation, and primary protocol estimands.

The measured origin is `S=50`. Every protocol creates measured request `r0` at `s0=S`. The one-move and two-move scenarios use the single common exogenous schedules `m1=S+1=51` and `(m1,m2)=(51,1052)`, respectively. Because the minimum frozen latency is 25 ticks, `r0` is outstanding at `m1` for every protocol and eligible core cell, and its observation has the old first target. The first recovery window ends at `m1+1000=1051`; the second move is applied one tick later, so neither the 2.0-second window nor the required 100 ms dwell for the first displacement is truncated by the second. The second window ends at `m2+1000=2052`, below the 3125-tick episode end. No claim is made that `r0` remains outstanding at `m2`, and observations captured after `m1` but before `m2` see only the first moved target. No A-specific or candidate-specific target schedule exists. Thus every candidate/A pair has exactly equal stack, seed, latency, `m1`, and `m2`, and one A rollout is reused rather than rerun across candidate contrasts. The pure schedule iterator, schedule hash, golden tests, and every seed-manifest `move_ticks` value must encode these exact ticks; changing them changes the study identity but not the eight core cells or any declared sample count.

Core cells are latency `ell in {25,75,150,350}` times one/two moves. A normal response has `d_j=s_j+ell`. Requests are admitted only when the number of outstanding requests at the start of the request phase is strictly less than the protocol cap `I`; acceptance at the delivery phase does not retroactively admit a request that tick.

## Protocol recurrences and transforms

| ID | Protocol | Measured request recurrence | Accepted-arrival transition |
|---|---|---|---|
| A | OPEN_LOOP | `s0=S` only | accept raw executable; no later request |
| B | RECEDING_PREFIX | `s_(j+1)=d_j+K_B` after issuing `K_B` 2 ms executable rows; `I=1` | discard suffix, accept raw; hold while absent |
| C | TEMPORAL_ENSEMBLE | desired epoch `e_j=S+jQ`; actual `s_j=min{t>=e_j: outstanding_start(t)<I}` | derive unissued absolute samples from valid normalized proposals |
| D | LATEST_VALID | same periodic recurrence as C | replace all unissued samples with newest normalized proposal |
| E | ASYNC_SAFE_PREFIX | `s_(j+1)=d_j+E-J_E`; `I=1` | prefetch before expiry; continue the valid future when feasible, then replace |
| F | OVERLAP_BLEND | same periodic recurrence as C | derive blended overlap then newest normalized suffix |
| G | RTC_APPROXIMATION | same periodic recurrence as C | derive committed-prefix-conditioned future then normalized suffix |

The periodic minimum is evaluated tick by tick, so a capped desired epoch is deferred, not dropped: if its oldest delivery is at `t`, it may fire no earlier than `t+1`. There is at most one admitted request per tick, and the invariant is exactly `outstanding<=I` after admission. Each admitted request captures a fresh observation; none is reused.

For E, `J_E` is a lead in low-level ticks, not B's prefix length. In a normal cell, the old executable ends at `d_j+E` and the successor arrives at `d_j+E-J_E+ell`. The cell is continuous without hold exactly when `ell<=J_E`, has strict old/new overlap exactly when `ell<J_E`, and has an unavoidable hold of exactly `ell-J_E` ticks when `ell>J_E`. Equality delivers the successor at the old half-open expiry and accepts it without an idle/hold tick. The frozen iterator labels every E cell `STRICT_OVERLAP`, `EXPIRY_HANDOFF`, or `UNAVOIDABLE_HOLD` from this equation; tests assert all three classes and forbid describing an unavoidable-hold cell as successful prefetch continuity.

Every delivered response first preserves the actual unchanged P4 policy output as immutable `policy_raw_i`: its exact nine-knot float64 C-order matrix, promoted representation, `dt_s=0.100`, source observation, and matrix SHA-256. The adapter then converts those sealed bytes, without execution, to a distinct 125-row normalized absolute-tick proposal `R_i(u)` with `dt_s=0.002`. Let nominal delivery be `g_i=s_i+ell` and actual response generation/delivery be `a_i`; they are equal except in a declared pause/delay. The response is not generated while the server is paused, so the normalized proposal's immutable half-open coverage is `U_i=[a_i,min(a_i+N,3125))`. P2 joint knots and P4 Cartesian knots use the frozen P4 linear interpolation at exact 2 ms ticks; no executor state, target update, or future observation enters this resampling. The validator must reconstruct all 125 normalized rows byte-for-byte from the sealed nine-knot policy matrix before accepting a normalized hash. Both payloads retain the same request observation as source, so pausing increases observation-to-action age without pretending the response existed earlier. The active non-hold executable before an arrival defines `O(u)` over its remaining half-open coverage only when it has the same representation and row width as the arriving normalized proposal; already issued ticks are never included. A broker safe hold, mismatched representation, or mismatched row width never defines overlap future for F/G, even though a hold remains executable until the proposal is accepted.

For C at broker transition tick `b`, contributors are `V_b={i:b in U_i}`. Order them by `(a_i,request_sequence,proposal_id)`. Set `z=min_i end(U_i)` for `i in V_b`; if `V_b` is empty, enter hold. For every integer `u in [b,z)`, `w_i(u)=exp(-lambda*((u-a_i)*D))` and `C(u)=sum_i(w_i(u)*R_i(u))/sum_i(w_i(u))`. Exact `lambda=0` gives an arithmetic mean. At `z`, the old derived chunk is half-open expired: clear it without `CHUNK_REPLACED`, remove expired parents, and immediately derive a new C chunk from the remaining parents before execution. This expiry-driven transition emits local `DERIVATION_RECOMPUTED` then canonical `CHUNK_ACCEPTED`, but no invented policy response. If no parent remains, enter hold. A newly delivered normalized proposal performs the same derivation after its real `POLICY_RESPONDED` event and replaces a compatible old chunk only when that old chunk is still half-open valid.

For F on arriving normalized proposal `r` at tick `b`, let `z=end(U_r)` and let `o=O` only when the compatible prior non-hold executable covers `b`. The overlap length is `h=min(M,z-b,end(O)-b)` when `o` exists and zero otherwise. For `j=0..h-1`, `beta=(j+1)/(M+1)` and `F(b+j)=(1-beta)O(b+j)+beta R_r(b+j)`; for `u in [b+h,z)`, `F(u)=R_r(u)`. For G use the same exact compatible old-future definition as its approximation's committed future: `c(u)=O(u)`, `h=min(L,z-b,end(O)-b)`, `gamma=(L-j)/L`, and `G(b+j)=gamma O(b+j)+(1-gamma)R_r(b+j)` for `j=0..h-1`; later rows are `R_r`. If no compatible non-hold old executable covers `b`, F and G use the normalized proposal without blending/conditioning. This makes G a transparent committed-future projection, not learned flow conditioning.

Every C/F/G derived chunk owns a new ID. Its normalized-parent proposal SHA-256 values are sorted in metadata. F/G source observation is the arriving proposal's observation. C source observation is the newest contributor by the frozen order above, including after expiry recomputation. `generated_time_ns=valid_from_ns=b*2,000,000`, `expires_at_ns=z*2,000,000`, and `actions.shape=(z-b,A)`. Parent coverages, owner observation, `b`, `z`, scalar/vector revision, and output SHA-256 are immutable sidecar fields. P4 limits, slew limiting, and PD control follow transforms. The committed-future projection defined here is always `RTC_APPROXIMATION`. It is descriptive/non-promotable; `RTC_COMPATIBLE` is a distinct execution path and is forbidden unless backed by the P3 provenance and conformance gate below. In that distinct path, the request serializes the exact immutable committed-prefix rows and SHA-256, the P3-provenanced policy conditions on those bytes, the response echoes the prefix hash, and the broker accepts the returned future directly without applying the approximation formula; a missing/mismatched echo is invalid evidence.

## Fault schedules

Drop, old-after-newer, pause, and discontinuity probes use `ell=150`, one move exactly one tick after the targeted request, all four pilot seeds, and only the first four confirmation seeds. Each probe starts from a fresh rollout. For A the target is `r0` at 50. For B it is the first post-`r0` request at `p=200+K_B`; for E it is the first post-`r0` request at `p=200+125-J_E`; and for C/D/F/G it is a frozen post-initial-acceptance request at `p=201`. Its normal delivery is `g=p+150`. Apart from the second request in old-after-newer, no other request is admitted in one of these probes. Drop creates no queued response or wrapper: active future exhausts, then hold. Pause retains a pending, not-yet-generated response, changes actual generation/delivery from `g` to `p+300`, assigns ordinary actual-delivery-anchored coverage `[p+300,min(p+300+N,3125))`, then processes it through the ordinary validity/out-of-order lifecycle. It is never modeled as a drop, an early-generated stale wrapper, or a nominal-delivery-anchored expiry.

The alternative-strategy probe has its own conflict schedule at `ell=25`; it is not silently placed on the preceding non-overlap schedule. It uses the same four pilot seeds and first four confirmation seeds, starts from a fresh rollout, and moves the target exactly one tick after its targeted request. The initial `r0` is delivered at 75 and covers `[75,200)`. A remains N/A. B targets `p=75+K_B`, after its receding prefix has deliberately discarded the old future, so its payload-validity probe runs but its overlap class is explicitly `OVERLAP_NA`. E targets its ordinary prefetch request at `p=75+125-J_E`: v0 arrives at 200 and is exactly `EXPIRY_HANDOFF`, while v1 arrives at 150 with 50 old-future ticks and v2 at 101 with 99 old-future ticks, both `POSITIVE_OVERLAP`. C/D/F/G target a frozen request at `p=76`, delivered at 101 while `r0` still covers `[101,200)`, for 99 positive-overlap ticks. All other requests are suppressed in this probe. At the delivery transition, tests require C's contributor set to be exactly `{r0,r1}`, D to replace a half-open-valid old future, and F/G to have `h>=1` for every vector. The positive-overlap proof and expected contributor/overlap counts are cell-table inputs; changing them changes the study identity. This keeps exactly one alternative cell wherever the prior count table declared one, while never claiming overlap reconciliation for A, B, or E-v0.

Alternative and discontinuity payloads use exact revision `exp02-fault-payload-v1`, change only the targeted normalized proposal, and keep its sealed original nine-knot P4 policy payload, request, move, nominal coverage, and actual-delivery ticks fixed. Let the byte-verified pre-fault normalized proposal have `n=125` rows `r_j`, `j=0..124`, in the promoted representation. For P2 define unit direction `v=(1,-1,1)/sqrt(3)` and hard envelope `[-2.70,2.70]^3`. For P4, let `a=r_(n-1)-r_0`; require `||a||_2>0`; define `v=(-a_1,a_0)/||a||_2`, and use P4's frozen workspace envelope. Choose sign `sigma` by trying `+1` then `-1`; the first sign for which every injected row is inside the applicable hard envelope wins. Neither sign fitting invalidates the fault fixture rather than clamping or changing amplitude.

For the alternative-strategy probe, use amplitude `c=0.05 rad` for P2 and `c=0.02 m` for P4 and set `r'_j=r_j+sigma*c*sin(pi*j/(n-1))*v`. Endpoints are unchanged, at least one interior row must differ bytewise, and this smooth detour is the second valid strategy. For the discontinuity probe, use `c=0.10 rad` for P2 and `c=0.03 m` for P4, set exact step index `j*=2`, and set `r'_j=r_j` for `j<j*` and `r'_j=r_j+sigma*c*v` for `j>=j*`; the bytewise step at `j*` must be nonzero. Both probes retain finite float64 C-order rows, record `fault_payload_revision`, `fault_step_index` (`null` for alternative), `fault_amplitude`, `fault_direction`, `fault_sign`, and pre/post normalized SHA-256 values, then pass through the ordinary C/F/G transform and unchanged P4 slew/PD/safety path. The cell-table hash covers the revision, formulas, amplitudes, sign order, envelopes, conflict-overlap class, expected contributor count, and expected transition. No RNG, live target, executor state, or outcome may affect either payload.

The old-after-newer probe is executable only for periodic C/D/F/G with `I>=2`. Freeze `s0=50,d0=200`, then `s1=d0+1=201`, `s2=s1+Q=251`, `d2=s2+150=401`, and delayed `d1=d2+1=402`. Thus `r0` has already been accepted, `r1` alone is outstanding when `r2` is admitted, `r2` arrives first, and `r1` is rejected out of order. It requires and never exceeds capacity two. It is N/A for `v0 (I=1)`, A, B, and E; N/A cells are absent, not imputed.

| protocol | drop | old-after-newer | pause | alternative | discontinuity |
|---|---|---|---|---|---|
| A | `r0` dropped; hold; safety-only | N/A | `r0` pending 150 extra ticks, then normal lifecycle | N/A | accept/account `r0` |
| B | first post-`r0` request dropped; hold | N/A | pending 150 extra ticks, then normal lifecycle | valid detour after deliberate prefix discard; overlap N/A | accept/account |
| C | `r1` dropped; ensemble then hold | exact `201/251/401/402` schedule when `I>=2` | pending 150 extra ticks, then normal lifecycle | exactly two-parent ensemble over 99 ticks | ensemble/account |
| D | `r1` dropped; future then hold | exact `201/251/401/402` schedule when `I>=2` | pending 150 extra ticks, then normal lifecycle | replace half-open-valid old future with `r1` | replace/account |
| E | first post-`r0` prefetch dropped; hold | N/A | pending 150 extra ticks, then normal lifecycle | v0 expiry handoff; v1/v2 positive overlap | replace/account |
| F | `r1` dropped; future then hold | exact `201/251/401/402` schedule when `I>=2` | pending 150 extra ticks, then normal lifecycle | blend with `h>=1` over old/new overlap | blend/account |
| G | `r1` dropped; future then hold | exact `201/251/401/402` schedule when `I>=2` | pending 150 extra ticks, then normal lifecycle | condition with `h>=1` over old/new overlap | condition/account |

Cells per seed are A=11, B=12, E=12, and each C/D/F/G=12 for v0 or 13 for v1/v2. The eight no-fault core cells are paired primary evidence; fault probes are safety/descriptive only. Exactly one stationary no-fault control runs for every frozen confirmation arm and each of the first four confirmation seeds; PRIMARY controls govern validity, while DESCRIPTIVE controls govern only their arm.

## Terminal cutoff and lifecycle

No protocol may create a request after tick `C=2500`. A natural trigger or deferred periodic epoch that first becomes admissible after C is canceled before observation/request creation. The cutoff is frozen from the worst path: `2500 + max normal ell 350 + pause extension 150 + E 125 = 3125`. Consequently every normal or paused response is delivered and every accepted executable expires by episode end. At terminal tick 3125 the delivery queue and broker queue must be empty; no active chunk remains. A deliberately dropped request has no delivery queue entry and is closed by a `dropped` disposition in the sidecar. Any other unresolved request, response, or queue entry invalidates the rollout.

Every request records proposal ID, rollout ID, request observation ID/time, request sequence, normal/actual delivery, representation, separate policy-raw and normalized `dt_s`/shape/SHA-256 identities, disposition, lifecycle/derived chunk IDs, and parent hashes in `results/<phase>/bundles/<identity>/proposals.jsonl`; every actually generated response preserves both its original nine-knot policy values and its 125-row normalized proposal in the adjacent typed tables below. A deliberately dropped request has neither payload and follows the exact null matrix below.

Direct A/B/D/E normalized proposals are executable. C/F/G normalized proposals remain sidecar records; original nine-knot policy payloads are evidence-only and never bypass normalization. A derived chunk starts at first unissued tick `b` and ends `z=min(normalized/executable coverage endpoint,3125)`, requiring `z>b`. It has `actions.shape=(z-b,A)`, `A=3` for P2 and `A=2` for P4; one absolute row per tick; `dt_s=.002`; validity `[b*2 ms,z*2 ms)`; and decoder row `current_tick-b` with no reinterpolation. Metadata has sorted normalized-parent hashes, rule/scalars, b/z, and owner (C newest contributor at b; F/G arriving proposal). P4 knots are sampled to this domain exactly once before any transform.

Every delivered rejected proposal creates an immutable wrapper `ActionChunk` with normalized shape/payload/source/validity and `metadata.origin=raw_rejected`; its `generated_time_ns` is the actual delivery time even when its retained normalized validity has already expired. `POLICY_RESPONDED` references the wrapper, followed by `CHUNK_REJECTED_EXPIRED` or `CHUNK_REJECTED_OUT_OF_ORDER`. A dropped request has no wrapper or payload-table rows; a paused request gets no wrapper or rows while pending and both policy/normalized rows plus one wrapper only when actually delivered. For C/F/G, the valid `POLICY_RESPONDED` references the delivery-derived executable chunk while both source payloads stay sidecar-only. A valid direct/derived response emits `POLICY_RESPONDED`, `CHUNK_REPLACED` only for a half-open-valid old chunk, then `CHUNK_ACCEPTED`. Expired old state clears before new acceptance; no nonexistent expiry event is invented.

At tick `b<3125` with no valid future, the P5-local representation dispatcher latches measured `q_ref=q(b)` and installs a broker-generated `JOINT_POSITION ActionChunk`. Its rows are `(3125-b,3)`, every row exactly `q_ref`, `dt_s=.002`, validity `[b*2 ms,6.25 s)`, `metadata.origin=broker_safe_hold`, and `metadata.hold_adapter=p5_joint_pd_latch_v1`. Its required `source_observation_id/time` are the most recent stored canonical observation at or before `b`; metadata separately records `hold_latched_tick=b` and the SHA-256 of measured q, so it never falsely presents that observation as a policy response. The dispatcher handles that origin before stack dispatch and calls P4's unchanged common joint-reference slew/PD/torque path directly; it never passes a 3-column hold through P4's P4/Cartesian decoder and never edits P4 source. It commands desired `dq=0`, invokes no differential IK or null-space posture, and produces ordinary immutable `ControlReference(controller_mode="p5_joint_pd_hold")` and `ACTION_EXECUTED` rows.

Hold installation emits canonical `CHUNK_REPLACED` only if a prior executable is still half-open valid, then `CHUNK_ACCEPTED`; exhaustion at an expiry first clears the expired executable and directly accepts hold. A hold is not periodically renewed. A later valid response may replace it, and a later distinct exhaustion may create one new hold with a new ID and newly latched q. At terminal tick 3125 the half-open hold is cleared before terminal assertions without an invented expiry event, so no active chunk remains. Replay resolves every hold reference/action to that hold ID and reproduces the same terminal inactive state. Regression tests assert exact q-reference latching, desired dq zero, no DIK call, bounded PD settling/error under the frozen P4 tolerance, no post-settling reference drift, second-hold behavior after a later replacement, and terminal clearing; they do not incorrectly require physical q/dq to be constant at hold entry.

Global invariants are: no invalid execution or old-observation supersession; finite shapes; bounded queue/inflight; immutable issued samples; safe hold rather than stale future; monotonic events; one fresh observation per request; addressable direct/derived/rejected/hold lifecycle; and no network, remote, or physical path.

## Pilot, freeze, confirmation, and multiplicity

Exactly three candidate vectors exist across all candidate execution: `v0=(K_B=1,I=1,lambda=0,M=1,L=1,J_E=25)`, `v1=(2,2,1,2,2,75)`, and `v2=(4,4,4,4,4,124)`. A is an untuned anchor and runs once per scenario. B reads only `K_B`; E reads only `J_E`; C/D/F/G read the named applicable coordinates, so no protocol silently reinterprets a coordinate. Timeout is absent: drop waits for active exhaustion then installs hold; pause has its finite delivery state machine above. A correction can replace a vector only before any pilot output exists; after pilot begins, any vector or schedule change ends the study and requires a reviewed new design. No protocol consumes more than these three vectors.

Before pilot, commit the code SHA, three vectors, tuning manifest, encrypted-or-hash-committed disjoint validation manifest, selection/tie rules, G role, and resource ceiling. Four tuning seeds run all three vectors for B--G independently on each eligible stack; four untouched validation seeds are revealed only after selecting each `(stack,protocol)` vector and run that vector. Within each stack/protocol, select the highest equal-weight eight-core recovery success among vectors with zero invariant failures; ties resolve v0, v1, v2. The `(stack,protocol)` candidate is a pilot survivor only if its selected vector also has zero invariant failures on all validation cells. A vector selected for P2 is never implicitly reused for P4, or conversely. G is frozen per stack as `DESCRIPTIVE_RTC_APPROXIMATION` unless the P3 evidence hash identifies an actual compatible flow policy for that exact representation, every G raw carries that P3 provenance, the broker emits canonical `RTC_COMPATIBLE`, and the pre-pilot conformance suite proves the real compatible execution path. Approximation-only G still runs the same cells and counts, but is outside the primary family, multiplicity, scientific label, and runtime selection. A proven compatible G on the PRIMARY stack is frozen as `PRIMARY_RTC_COMPATIBLE` and is treated like the other PRIMARY candidates; a compatible G on the DESCRIPTIVE stack remains descriptive. No source/config edit is allowed after the first pilot output; any correction stops P5 and becomes a separately reviewed follow-up rather than a second autonomous P5 pilot revision. Any A invariant or safety failure stops that eligible stack, not a different stack or merely one protocol. Once eligible pilot execution has begun, PRIMARY A failure is scientific `INCONCLUSIVE` with no authority whether it occurs in tuning or validation; DESCRIPTIVE A failure invalidates only that descriptive arm. Candidate failures stop that stack-qualified candidate. Zero authority-eligible PRIMARY survivors after a valid PRIMARY A pilot freezes an operational `NOT_SUPPORTED` decision with no confirmation or efficacy claim. Its required deterministic protocol selection is the valid baseline `OPEN_LOOP`, while `promotion_state=NO_SUPPORTED_PROTOCOL` and empty runtime authority make explicit that no novel protocol was promoted and that the pilot-only fallback has no confirmation-backed runtime authority. Descriptive evidence cannot prevent that outcome.

After the invariant pilot, freeze the PRIMARY authority-eligible survivor rows and family size `m` once. Also freeze DESCRIPTIVE arm rows separately: the DESCRIPTIVE A plus each of that stack's pilot survivors, and approximation-only G for either stack when it survived. If `m>0`, run PRIMARY A once, all `m` PRIMARY survivors, every frozen descriptive arm, and no other candidate; do not shrink either inventory after seeing confirmation. The maximum PRIMARY family is five when PRIMARY G is approximation-only and six only when it passed the real `RTC_COMPATIBLE` provenance/conformance gate. Approximation-only G and every DESCRIPTIVE-stack arm do not enter `m`; their declared rollout maxima remain reserved. Family-wise `alpha=0.05`. Each PRIMARY candidate/A paired percentile-bootstrap interval uses deterministic 10,000 resamples and Bonferroni marginal confidence `1-alpha/m`, equivalently lower and upper probabilities `alpha/(2m)` and `1-alpha/(2m)`. A stopped/invalid PRIMARY confirmation makes the PRIMARY study `INCONCLUSIVE`; a failed PRIMARY candidate remains in the frozen family as a non-passing contrast. Invalid descriptive evidence is reported against that arm but cannot change an otherwise valid PRIMARY scientific label or authority decision.

The sole count source is:

| protocol class | cells/seed by config | pilot tuning | pilot validation max | pilot max | confirmation core | confirmation probes, first 4 seeds | controls, first 4 seeds | confirmation max |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| A anchor | 11 | 4x11 | 4x11 | 88 | 32x8=256 | 4x3=12 | 4 | 272 |
| B or E | 12 per vector | 4x(12+12+12)=144 | 4x12=48 | 192 each | 256 | 4x4=16 | 4 | 276 each |
| C/D/F/G | v0=12; v1/v2=13 | 4x(12+13+13)=152 | 4x13=52 | 204 each | 256 | at most 4x5=20 | 4 | 280 each |

Thus pilot maximum per eligible stack is `88 + 2*192 + 4*204 = 1288` rollouts, and every protocol is <=204 pilot rollouts. Confirmation maximum per stack is `272 + 2*276 + 4*280 = 1944`; two eligible stacks give 3888 confirmation rollouts. One candidate has 256 paired core episodes and at most 280 total confirmation episodes including probes/control, below the inherited 1,024 paired-confirmation ceiling. Counts reserve G even when it is descriptive and conservatively reserve every candidate even though only frozen survivors execute. The PRIMARY claim uses only the PRIMARY stack and authority-eligible family. DESCRIPTIVE stacks, approximation-only G, and valid probe outcomes never affect the scientific label. Invalid or missing PRIMARY probe evidence follows the PRIMARY fault rules below; invalid or missing DESCRIPTIVE evidence invalidates only that descriptive arm and is never allowed to contaminate the PRIMARY decision.

Freeze hashes P2--P4 evidence, eligibility proof, unchanged code SHA, every stack-qualified selected vector and survivor disposition, the PRIMARY family/m, separate DESCRIPTIVE arms, cell table, the exact fixed margins/gates below, metrics/bootstrap, and only the confirmation RNG algorithm/candidate procedure/count 32 before deriving the immutable unseen confirmation manifest. Confirmation seed values must not be read, materialized, or logged before that freeze.

## Resources, evidence, and resume

All maxima derive from the count table and a 6.25-second episode:

| scope | rollout max | simulated seconds | episode-bundle bytes at <=1 MiB each | report allowance |
|---|---:|---:|---:|---:|
| pilot, one stack | 1288 | 8050 | 1288 MiB | 128 MiB |
| confirmation, one stack | 1944 | 12150 | 1944 MiB | 128 MiB |
| complete phase, two stacks | `2*(1288+1944)=6464` | 40400 | 6464 MiB, including both proposal-payload tables | 512 MiB |

P5 deliberately narrows the inherited two-revision allowance to exactly one retained autonomous pilot protocol revision. A correction after pilot output stops this study and may be proposed for later review, but the autonomous P5 run neither starts nor retains a second revision. The two-stack completed-evidence maximum is therefore `6464 + 512 = 6976 MiB`. Quarantine is separately capped at 512 MiB and the one serial in-progress shard may consume at most 14 MiB of same-parent temporary bundles, giving the exact retained-plus-live maximum `6976 + 512 + 14 = 7502 MiB`, below 10 GiB. Crossing either sub-cap stops before creating another byte and is `INCONCLUSIVE`, never `NOT_SUPPORTED`; quarantine is never deleted autonomously. Every complete outer episode bundle—including canonical rollout, both proposal-payload tables, every sidecar, and bundle manifest—is checked at <=1,048,576 bytes before publication; nothing is budgeted off-ledger. The periodic request maximum is 50, so `policy-raw-actions.parquet` has at most `50*9=450` rows and `450*3*8=10800` uncompressed float payload bytes, with a 65,536-byte serialized sub-cap. `normalized-proposals.parquet` has at most `50*125=6250` rows and `6250*3*8=150000` uncompressed float payload bytes, with the existing 262,144-byte serialized sub-cap. Both sub-caps are included inside, not added to, the 1 MiB bundle cap. The recalculated complete-evidence bound therefore remains exactly `6464*1 MiB=6464 MiB`; policy and normalized proposal payloads do not create an off-ledger additive allowance. Preflight uses descriptor-relative sizes to reserve the full declared next stage plus all retained final, temporary, and quarantine bytes and fails closed if either payload-table, bundle, stage, quarantine, live-temporary, or 7502 MiB total cap would be exceeded.

A pilot shard `(phase,stack,protocol,vector-or-anchor,seed)` has at most 13 episodes/81.25 simulated seconds; a confirmation shard `(phase,stack,protocol,seed)` has at most 14 episodes/87.5 seconds. Every command has a hard 60-minute wall timeout and create-only checkpoint. The complete tuning/select/validation pilot pass has hard caps of 24 wall-clock hours and 60 aggregate CPU hours; freeze has 1 wall/2 CPU hours; confirmation has 24 wall/60 CPU hours; analyze has 30 minutes wall/1 CPU hour; report has 30 minutes wall/1 CPU hour. Thus P5 preserves exact maxima of 50 wall-clock hours across its serial passes and 124 aggregate CPU hours, inside the inherited 24-hour-per-pass and 240-CPU-hour complete-core ceilings. The complete tracked report transaction, including its marker, is capped at 1 MiB and remains inside both the 512 MiB report allowance and 14 MiB live-temporary cap. Host monotonic wall and process-plus-child CPU counters are written after every shard; preflight subtracts accumulated use, and timeout/cap exhaustion publishes only a `DECLARED_MISSING` tombstone when its rules below apply and otherwise yields a blocker.

The sole runner is `python -m experiments.02_action_chunks.run`. All paths below are repository-root relative and are rejected if absolute, escaping, symlinked, or not the declared manifest path. Live commands call `SafetyConfig` and the P3/P4 evidence gate before importing MuJoCo or creating output. The runner exposes exactly `pilot`, `freeze`, `confirmation`, `analyze`, and physics-free `report`. Pilot additionally requires exactly one `--pilot-stage tuning|select|validation`; tuning and validation are physics shards, while select is a physics-free one-shot reduction. A manifest driver enumerates every declared shard and substitutes its literal stack/protocol/vector/seed shard fields into these same shapes; there is no alternate batch API. `--seed` is an assertion, never a seed source: on a physics shard it is required to equal the single manifest-derived integer seed and otherwise is forbidden except with `--dry-run`. With `--dry-run`, a supplied exact integer seed must occur in the declared manifest and filters printed identities to that seed without changing the manifest, resource reservation, or any later executable identity. `--dry-run` is valid for every phase, performs all gates/schema/path/resource/identity checks, prints canonical planned identities and byte ceilings, imports no MuJoCo, creates no file, and cannot be combined with report publication. Omission of `--seed` preserves the manifest-only autonomous path. Exact command shapes and stage order are:

```text
UV_CACHE_DIR=.cache/uv uv run python -m experiments.02_action_chunks.run --phase pilot --pilot-stage tuning \
  --config experiments/02_action_chunks/configs/pilot-r1.yaml \
  --seed-manifest experiments/02_action_chunks/protocol/pilot-r1/tuning-seeds.json \
  --stack-id P2 --protocol B --vector v0 --shard-index 0 --shard-count 4 \
  --output-dir experiments/02_action_chunks/results --headless --max-episodes 12

UV_CACHE_DIR=.cache/uv uv run python -m experiments.02_action_chunks.run --phase pilot --pilot-stage select \
  --config experiments/02_action_chunks/configs/pilot-r1.yaml \
  --seed-manifest experiments/02_action_chunks/protocol/pilot-r1/tuning-seeds.json \
  --validation-commitment experiments/02_action_chunks/protocol/pilot-r1/validation-seeds.json \
  --validation-secret experiments/02_action_chunks/private/validation-seeds.secret.json \
  --output-dir experiments/02_action_chunks/results \
  --selection-output experiments/02_action_chunks/protocol/pilot-r1/pilot-selection.json \
  --validation-seed-output experiments/02_action_chunks/protocol/pilot-r1/validation-seeds.revealed.json \
  --headless

UV_CACHE_DIR=.cache/uv uv run python -m experiments.02_action_chunks.run --phase pilot --pilot-stage validation \
  --config experiments/02_action_chunks/configs/pilot-r1.yaml \
  --seed-manifest experiments/02_action_chunks/protocol/pilot-r1/validation-seeds.revealed.json \
  --selection-manifest experiments/02_action_chunks/protocol/pilot-r1/pilot-selection.json \
  --stack-id P2 --protocol B --shard-index 0 --shard-count 4 \
  --output-dir experiments/02_action_chunks/results --headless --max-episodes 12

UV_CACHE_DIR=.cache/uv uv run python -m experiments.02_action_chunks.run --phase freeze \
  --config experiments/02_action_chunks/configs/pilot-r1.yaml \
  --seed-manifest experiments/02_action_chunks/protocol/pilot-r1/validation-seeds.revealed.json \
  --selection-manifest experiments/02_action_chunks/protocol/pilot-r1/pilot-selection.json \
  --output-dir experiments/02_action_chunks/results \
  --frozen-output experiments/02_action_chunks/configs/frozen.yaml \
  --confirmation-seed-output experiments/02_action_chunks/protocol/confirmation/seed-manifest.json \
  --headless

UV_CACHE_DIR=.cache/uv uv run python -m experiments.02_action_chunks.run --phase confirmation \
  --config experiments/02_action_chunks/configs/frozen.yaml \
  --seed-manifest experiments/02_action_chunks/protocol/confirmation/seed-manifest.json \
  --stack-id P2 --protocol B --shard-index 0 --shard-count 32 \
  --output-dir experiments/02_action_chunks/results --headless --max-episodes 13

UV_CACHE_DIR=.cache/uv uv run python -m experiments.02_action_chunks.run --phase analyze \
  --config experiments/02_action_chunks/configs/frozen.yaml \
  --seed-manifest experiments/02_action_chunks/protocol/confirmation/seed-manifest.json \
  --output-dir experiments/02_action_chunks/results --headless

UV_CACHE_DIR=.cache/uv uv run python -m experiments.02_action_chunks.run --phase report \
  --config experiments/02_action_chunks/configs/frozen.yaml \
  --seed-manifest experiments/02_action_chunks/protocol/confirmation/seed-manifest.json \
  --output-dir experiments/02_action_chunks/results --headless
```

Candidate pilot tuning requires `--vector`; A anchor shards forbid it and use `vector_id=null`. Pilot validation and confirmation forbid `--vector` and resolve the create-only selected vector for B--G, while A remains the untuned anchor. Pilot select requires `--selection-output`, `--validation-commitment`, `--validation-secret`, and `--validation-seed-output`; forbids stack/protocol/vector/shard flags; validates every tuning shard before choosing; atomically publishes the exact selection schema below; then reads the ignored root-relative secret, verifies its canonical-byte SHA-256 commitment, and create-only materializes the validation seed file. Both output targets must initially be absent, and the seed output is not created until the selection file is fsynced and revalidated. The secret has the revealed seed-manifest schema, is never copied into results, and a missing/mismatched/non-ignored secret fails closed. Pilot validation requires `--selection-manifest`. Freeze/analyze/report forbid stack/protocol/shard flags and run no physics. Freeze validates all tuning/selection/validation bundles, writes the frozen protocol create-only, fsyncs and reopens it to verify its hash, then and only then derives the new confirmation RNG root and create-only seed manifest; failure between those operations leaves no confirmation seeds and resume validates the existing frozen protocol before generation. `frozen.yaml` is canonical JSON-as-YAML: UTF-8, sorted object keys, compact separators, no aliases/tags/comments, finite JSON numbers only, and one trailing LF. Validation uses a duplicate-key-rejecting safe loader, reserializes through that exact profile, and requires byte equality before its SHA-256 can seed bootstrap. `--shard-index` is zero-based and strictly less than `--shard-count`; the manifest records and requires the exact count. The pure sorted manifest iterator is the sole source of stack/protocol/vector/seed/cell identities and exact shard episode count. `--max-episodes` is smoke-only unless it equals that complete count. A v1/v2 periodic pilot shard therefore uses 13 rather than the B example's 12; confirmation uses 11/12/13/14 according to the manifest, with 14 only for a first-four periodic seed containing five probes plus control.

The exact `frozen.yaml` top-level keys are `schema_version,study_id,implementation_git_sha,parent_protocol_hash,evidence_hashes,p1_provenance,p4_provenance,eligible_stacks,stack_protocol_selections,primary_family,family_size,descriptive_arms,cell_table,schedule,metrics,gates,bootstrap,seed_hashes,resource_limits`. `evidence_hashes` has exactly `p1_module_ledger,p1_api_conformance,p2,p3,p4_p3_gate,p4_frozen,p4_frozen_digest,p4_confirmation_seed,p4_confirmation_protocol,p4_preregistration_binding,p4_analyze_artifact_manifest,p4_artifact_digests,p4_decision,p4_implementation_allowlist,adapter`. `p1_provenance` has exactly `implementation_git_sha,module_ledger,module_ledger_hash,api_conformance_hash`; its ledger uses the seven exact rows and schema above. `p4_provenance` has exactly `implementation_git_sha,preregistration_git_sha,publication_git_sha,implementation_allowlist_paths,implementation_allowlist_hash,p3_gate_git_blob_sha1,p3_gate_sha256`, with sorted unique root-relative paths and lowercase SHAs. `eligible_stacks` is one or two rows ordered PRIMARY then DESCRIPTIVE; each row has exactly `stack_id,representation,role`, role is `PRIMARY|DESCRIPTIVE`, and the stable-first P4 order determines the roles. `stack_protocol_selections` is ordered by `(stack_id,protocol_id)` and has one row for every eligible stack and A--G with exact keys `stack_id,protocol_id,selected_vector_id,pilot_survivor,g_role`; A has null vector and `g_role=null`, B--F have `g_role=null`, and G has `DESCRIPTIVE_RTC_APPROXIMATION|PRIMARY_RTC_COMPATIBLE|DESCRIPTIVE_RTC_COMPATIBLE`. `pilot_survivor` is a strict boolean and A uses it to record anchor validity. `primary_family` is the ordered B--G subset of PRIMARY rows with `pilot_survivor=true` and an authority-eligible G role; each row has exactly `stack_id,protocol_id,selected_vector_id`, and `family_size` is the exact integer length of that array. `descriptive_arms` is ordered by `(stack_id,protocol_id)` and has exact row keys `stack_id,protocol_id,selected_vector_id,reason`; `reason` is `DESCRIPTIVE_STACK|RTC_APPROXIMATION`, and no identity may occur in both arrays. Each `cell_table` row has exactly `cell_id,role,move_count,move_ticks,latency_ticks,fault,target_request_sequence,control,e_continuity_class,conflict_overlap_class,expected_overlap_ticks,expected_contributor_count,fault_payload_revision`; `e_continuity_class` is `STRICT_OVERLAP|EXPIRY_HANDOFF|UNAVOIDABLE_HOLD` only for E core cells and null otherwise. `conflict_overlap_class` is `POSITIVE_OVERLAP|EXPIRY_HANDOFF|OVERLAP_NA` only for an alternative cell and null otherwise; `expected_overlap_ticks` is respectively a positive integer, zero, or null, and `expected_contributor_count=2` only for C positive overlap and otherwise null. `fault_payload_revision` is `exp02-fault-payload-v1` only for alternative/discontinuity cells and null otherwise. Rows use canonical ordering by the preceding fields. `schedule` has exactly `tick_ns,episode_end_tick,request_cutoff_tick,m1_tick,m2_tick,recovery_window_ticks,dwell_ticks,conflict_latency_ticks,conflict_initial_delivery_tick,conflict_periodic_request_tick`; the final three values are exactly `25,75,76`. `metrics` has exactly `episode_schema_hash,aggregate_schema_hash,primary_rule_hash,secondary_rule_hash,stationary_control_rule_hash,policy_raw_schema_hash,normalized_proposal_schema_hash`; `gates` has exactly `alpha,delta,precision_width_max,age_p95_max_s,hold_excess_max,jerk_ratio_max,discontinuity_ratio_max,stationary_error_max_m`; `bootstrap` has exactly `algorithm,resamples,seed_derivation,quantile_rule`; `seed_hashes` has exactly `tuning,validation_commitment,validation_reveal,confirmation`; and `resource_limits` has exactly `bundle_bytes,policy_raw_actions_bytes,normalized_proposals_bytes,completed_bytes,quarantine_bytes,temp_bytes,total_bytes,pilot_wall_seconds,pilot_cpu_seconds,freeze_wall_seconds,freeze_cpu_seconds,confirmation_wall_seconds,confirmation_cpu_seconds,analyze_wall_seconds,analyze_cpu_seconds,report_wall_seconds,report_cpu_seconds,report_bytes`. Arrays are sorted and unique; booleans and integers require exact JSON types; all nested null matrices above are enforced; no unknown or missing key is accepted.

The clean-tree lifecycle has two explicit Git identities and no circular self-hash. Before pilot, the tracked pilot config, tuning seeds, validation commitment, code, exact reviewed placeholders for `RESULTS.md` and `INTERFACE_FINDINGS.md`, immutable `README.md,CLAIM.md,EXPERIMENT.md`, and `.gitignore` exact entries for `experiments/02_action_chunks/private/`, the four generated freeze paths below, `protocol/confirmation/report-manifest.json`, and `ACTION_EXECUTION_DECISION.md` are committed at `implementation_git_sha`; the secret and each generated path must pass `git check-ignore` before output. Generated bundles remain under the already ignored `results/`. Pilot select/validation and freeze may create only `configs/frozen.yaml`, `protocol/pilot-r1/pilot-selection.json`, `protocol/pilot-r1/validation-seeds.revealed.json`, and `protocol/confirmation/seed-manifest.json` at their declared times; because those paths are initially ignored, every stage-entry clean-tree check remains literal. After freeze revalidates them, the orchestrator uses an explicit four-path force-add and commits exactly those files without source edits. Confirmation records that commit as `execution_git_sha` in its protocol manifest and refuses to run unless HEAD equals it and `git status --porcelain=v1 --untracked-files=all` is empty. The frozen file records the earlier implementation SHA, avoiding an impossible self-referential commit hash. Analyze creates ignored evidence only. Report starts from that same clean execution HEAD, validates analysis and all acceptance checks, atomically replaces only the two byte-exact reviewed placeholders and creates only `ACTION_EXECUTION_DECISION.md` plus `protocol/confirmation/report-manifest.json`; the orchestrator force-adds and commits exactly those four paths separately from source. `working_tree_clean` is the literal stage-entry result before that stage creates output and must be true. Any other repository-path write or any tracked/untracked non-ignored path at pilot, freeze, confirmation, analyze, or report entry fails closed.

Each episode publishes one outer bundle with exact inventory:

```text
bundle/
  rollout/                 # unchanged canonical RolloutWriter directory
  exp02/proposals.jsonl
  exp02/policy-raw-actions.parquet
  exp02/normalized-proposals.parquet
  exp02/derived-chunks.jsonl
  exp02/issued-references.parquet
  exp02/broker-lifecycle.jsonl
  exp02/episode-metrics.json
  bundle-manifest.json
```

`proposals.jsonl` has exact keys `proposal_id,rollout_id,request_observation_id,request_time_ns,request_sequence,normal_delivery_tick,actual_delivery_tick,delivery_mode,representation,policy_shape,policy_dt_s,policy_actions_sha256,normalized_shape,normalized_dt_s,normalized_actions_sha256,coverage_start_tick,coverage_end_tick,disposition,wrapper_chunk_id,derived_chunk_ids,parent_sha256s,fault_payload_revision,fault_step_index,fault_amplitude,fault_direction,fault_sign,pre_fault_normalized_sha256`; `delivery_mode` is `normal|paused|dropped`, while final disposition is exactly `accepted|expired|out_of_order|dropped`. A dropped row requires `delivery_mode=dropped`, `actual_delivery_tick,representation,policy_shape,policy_dt_s,policy_actions_sha256,normalized_shape,normalized_dt_s,normalized_actions_sha256,coverage_start_tick,coverage_end_tick,wrapper_chunk_id` and all six fault fields null, with empty `derived_chunk_ids,parent_sha256s`; its known counterfactual `normal_delivery_tick` remains non-null. A non-dropped row requires non-null actual delivery, representation, `policy_shape=[9,A]`, `policy_dt_s=0.100`, policy hash, `normalized_shape=[125,A]`, `normalized_dt_s=0.002`, normalized hash, and coverage. Non-injected proposals require all six fault fields null; alternative/discontinuity proposals require the exact populated matrix above, with only alternative `fault_step_index=null`. Parent hashes always name post-fault normalized proposal bytes, never policy-raw bytes.

`policy-raw-actions.parquet` preserves every non-dropped unchanged P4 policy response, including responses whose normalized proposal is sidecar-only or rejected, with exact Arrow schema `proposal_id:string,row_index:int32,row_width:int8,value_0:float64,value_1:float64,value_2:float64 nullable`. Rows sort by `(proposal_id,row_index)`, each proposal has exactly contiguous indices `0..8`, `row_width` equals 2 for P4 or 3 for P2, and `value_2` is null iff width is 2. `policy_actions_sha256` is SHA-256 over the reconstructed C-contiguous little-endian float64 matrix bytes. `normalized-proposals.parquet` uses the same column schema, preserves every non-dropped normalized proposal after any declared fault transform, and requires contiguous indices `0..124`; its reconstructed little-endian C-order matrix hashes to `normalized_actions_sha256`. For an injected proposal, the validator reverses the exact sealed transform, requires the recovered 125 rows to hash to `pre_fault_normalized_sha256`, and independently derives those pre-fault rows from the nine policy knots. Both tables require finite values and use the pinned PyArrow version, ZSTD level 9, no dictionary encoding, data page v1, and one row group; schema metadata are respectively `{schema_version:exp02-policy-raw-actions-v1}` and `{schema_version:exp02-normalized-proposals-v1}`, with byte caps 65,536 and 262,144. A dropped request has rows in neither table.

`derived-chunks.jsonl` has `chunk_id,rule,owner_observation_id,owner_proposal_id,parent_sha256s,b,z,representation,dt_s,shape,actions_sha256,scalar_revision,transition_reason`, where transition is `delivery|parent_expiry|safe_hold`; safe hold uses `rule=SAFE_HOLD` and null owner/proposal with an empty parent list. `issued-references.parquet` has typed columns `tick:int64,time_ns:int64,source_chunk_id:string,source_observation_id:int64,q_ref:fixed_size_list<float64>[3],dq_ref:fixed_size_list<float64>[3],representation:string,reference_sha256:string,is_hold:bool`; rows are unique and strictly tick-ordered. `broker-lifecycle.jsonl` has `tick,sequence,event,request_id,proposal_id,chunk_id,old_chunk_id,queue_depth,outstanding_count,reason`; local events include `DERIVATION_RECOMPUTED`, `REQUEST_DROPPED`, and `TERMINAL_CLEARED` and cross-link the canonical events rather than extending `ExecutionEventType`.

`episode-metrics.json` has exactly `schema_version,identity,move_count,displacement_success,displacement_completion_s,recovery_event_count,recovery_numerator,recovery_denominator,recovery_score,completion_n,completion_censored_n,completion_mean_s,stationary_error_n,stationary_error_max_m,stationary_control_pass,age_n,age_p50_s,age_p95_s,age_p99_s,execution_ticks,idle_ticks,idle_fraction,replacement_count,invalidated_action_rows,jerk_n,jerk_p95,discontinuity_n,discontinuity_p95,request_count,measured_duration_s,request_rate_hz,queue_overrun_count,safe_hold_activation_count,hold_ticks,total_core_ticks,unsafe_count,joint_limit_count,terminal_empty,warmup_valid,validity`. Core/fault displacement arrays have length one or two in move order. A stationary control has exact `move_count=0`, both displacement arrays empty, `recovery_event_count=0`, `recovery_numerator=0`, `recovery_denominator=0`, `recovery_score=null`, all completion counts zero, and `completion_mean_s=null`; no other valid cell may use zero denominator. A successful completion is seconds from its move to the first completed 100 ms dwell, while valid non-recovery is `success=0,completion=null` and increments `completion_censored_n`. Completion mean uses successful finite completions only and is null exactly when `completion_n=0`.

For a stationary control, `stationary_error_n` is exactly the 2,375 500 Hz samples at ticks `[750,3125)`, `stationary_error_max_m` is the maximum finite Euclidean EEF error over that domain, and `stationary_control_pass` is true exactly when the maximum is at most `0.025`, `recovery_event_count=0`, and every lifecycle/safety invariant passes. Core/fault cells instead require these three stationary fields null. Thus the unchanged P4 rule—error at most `0.025 m` from `t=1.5 s` through the last executed episode tick and no recovery event—is evidence-bearing and mechanically reproducible rather than inferred from 100 Hz telemetry. Equality passes. Action age is `ACTION_EXECUTED.time-source_observation_time` over non-hold 100 Hz stored samples in `[S,3125)`; all three percentiles use nearest rank and are null only when `age_n=0`, which invalidates a core episode. `execution_ticks` counts expected 500 Hz ticks in `[S,3125)` and `idle_ticks` counts those lacking exactly one valid `ACTION_EXECUTED`; safe-hold execution is not idle. Replacement count is canonical `CHUNK_REPLACED` after S. `invalidated_action_rows` counts unique, previously accepted, unissued `(chunk_id,absolute_tick)` rows canceled by replacement, excluding natural expiry and never-accepted raw rows. `request_count` counts measured `POLICY_REQUESTED` after setup, `measured_duration_s=(3125-S)*.002=6.15`, and request rate is their quotient. Safe-hold activations count distinct accepted `broker_safe_hold` chunks; hold ticks count their executed ticks. Jerk/discontinuity retain the P4 domains; queue/safety counts are exact event/telemetry counts. All denominators and null rules are validated, and no absent metric is converted to zero.

Every JSONL line is canonical JSON with fixed exact keys, finite values, LF ending, and no unknown field. The bundle validator requires unique IDs, sorted parent hashes, action/reference byte hashes, canonical rows in both payload tables for every non-dropped proposal and none for a drop, policy-to-normalized-to-derived-to-issued coverage, canonical event ordering, source observation ownership, wrapper/rejection links, hold dispatch, terminal inactive replay, and byte-equal immutable issued rows after every later delivery. It reconstructs every nine-knot policy matrix from `policy-raw-actions.parquet`, verifies its policy hash, applies only the frozen P4 interpolation to reproduce the pre-fault 125-row proposal, applies and replays any declared fault transform, verifies `normalized-proposals.parquet` byte-for-byte, then replays C/F/G and requires byte-equal derived matrices and issued references. A parent hash without matching normalized bytes, or normalized bytes without a matching policy-raw derivation, is invalid evidence. The manifest has exact keys `schema_version,identity,protocol_hash,source_hashes,files,total_bytes`; `files` lists every other regular file with relative path, media type, bytes, and SHA-256, excludes itself and temporary files, and has no self-digest.

Stage inventories are closed rather than pretending that every artifact exists in every phase; ignored evidence for each stage is rooted separately at `results/<phase>/<stage>/`, so names never collide. A failed prerequisite gate has only the separate preflight status inventory above. Pilot tuning publishes `protocol-manifest.json,tuning-seed-manifest.json,shard-manifest.jsonl,missing-tombstones.jsonl,tuning-aggregate.csv,artifact-manifest.json`; pilot select creates the tracked `pilot-selection.json,validation-seeds.revealed.json` and publishes its ignored `artifact-manifest.json`; pilot validation publishes `protocol-manifest.json,validation-seeds.revealed.json,shard-manifest.jsonl,missing-tombstones.jsonl,validation-aggregate.csv,artifact-manifest.json`. Freeze creates only tracked `configs/frozen.yaml,protocol/confirmation/seed-manifest.json`, validates the two prior tracked pilot files, and the orchestrator commits that four-file freeze set; it also publishes ignored `protocol-manifest.json,artifact-manifest.json`. Confirmation publishes `protocol-manifest.json,seed-manifest.json,shard-manifest.jsonl,missing-tombstones.jsonl,artifact-manifest.json` and bundles. Analyze alone publishes ignored `aggregate.csv,bootstrap.json,decision.json,artifact-manifest.json`; bootstrap is absent when `m=0`, in which case decision records an empty contrast array. Report alone publishes tracked `RESULTS.md,INTERFACE_FINDINGS.md,ACTION_EXECUTION_DECISION.md,protocol/confirmation/report-manifest.json`. No preflight/pilot/freeze artifact masquerades as a confirmation decision, and no analyze invocation writes a report.

`protocol-manifest.json` has exact keys `schema_version,phase,stage,study_id,implementation_git_sha,execution_git_sha,working_tree_clean,p1_provenance,p2_hash,p3_hash,p4_evidence_hashes,p4_provenance,adapter_hash,config_hash,vector_hashes,g_roles,schedule_hash,metric_hash,gate_hash,seed_hashes,resource_limits`. `p1_provenance`, `p4_evidence_hashes`, and `p4_provenance` are byte-equal to the frozen objects above, and entry/exit checks must reproduce their hashes before a stage marker is publishable. `g_roles` is an ordered array with exact row keys `stack_id,g_role`, one row per eligible stack, using the G enums above. `stage` is `tuning|select|validation|freeze|confirmation|analyze`; `seed_hashes` always has exact keys `tuning,validation_commitment,validation_reveal,confirmation`. At tuning, only tuning and commitment are non-null; after select and through validation, reveal is also non-null; freeze/confirmation/analyze require all four. `execution_git_sha` equals the implementation SHA through validation and the clean freeze commit for confirmation/analyze; report consumes that confirmation/analyze protocol identity and uses its distinct final marker schema below. `seed-manifest.json` has exact keys `schema_version,phase,rng_algorithm,rng_root_commitment,rng_root,partition,scenario_count,scenario_records`; `scenario_records`, when revealed, is an ordered array whose row keys are exactly `seed,scenario_id,move_count,latency_ticks,fault,target_request_sequence,move_ticks`. Pilot validation keeps `rng_root` and `scenario_records` null in the commitment file and never overwrites it: reveal is a distinct create-only `validation-seeds.revealed.json` with the same schema and matching commitment. Confirmation forbids its seed file before freeze. `pilot-selection.json` has exact keys `schema_version,study_id,tuning_protocol_hash,tuning_seed_hash,stack_selections,validation_commitment_hash,g_roles`; each ordered stack-selection row has exactly `stack_id,protocol_id,selected_vector_id,objective,valid_cell_count,tie_rank`. A has null vector/objective/tie rank and `valid_cell_count=44`; B--G have a selected vector, finite objective in `[0,1]`, `valid_cell_count=48` for B/E or 48/52 according to the selected periodic vector's 12/13 cells, and `tie_rank=0|1|2` for v0/v1/v2. Survival is intentionally absent because selection precedes untouched validation; Freeze derives `pilot_survivor` only after validating those later cells. `g_roles` has the same stack-qualified schema as the protocol manifest. Each `shard-manifest.jsonl` row has exact keys `shard_id,phase,pilot_stage,stack_id,protocol_id,vector_id,seed,cell_ids,episode_count,protocol_hash,seed_hash,output_identity,disposition`; disposition is exactly `COMPLETE|DECLARED_MISSING|INVALID`. Non-pilot `pilot_stage` is null, and sorted shard rows are unique and cover every declared identity exactly once. `artifact-manifest.json` uses exact keys `schema_version,phase,stage,protocol_hash,files,total_bytes`, excludes itself/no self-digest, and lists every phase file with exact entry keys `path,media_type,bytes,sha256`.

The exact aggregate header is `phase,stack_id,protocol_id,vector_id,seed,cell_id,move_count,recovery_event_count,recovery_numerator,recovery_denominator,recovery_score,completion_n,completion_censored_n,completion_mean_s,stationary_error_n,stationary_error_max_m,stationary_control_pass,age_n,age_p50_s,age_p95_s,age_p99_s,execution_ticks,idle_ticks,idle_fraction,replacement_count,invalidated_action_rows,jerk_n,jerk_p95,discontinuity_n,discontinuity_p95,request_count,measured_duration_s,request_rate_hz,queue_overrun_count,safe_hold_activation_count,hold_ticks,total_core_ticks,unsafe_count,joint_limit_count,terminal_empty,warmup_valid,bundle_sha256,validity`. It has one row per declared cell in canonical identity order. Valid stationary rows use the exact zero-move/null matrix above; valid nonstationary rows require stationary fields null. A validated bundle produces `validity=VALID` and no null required metric outside its declared cell-specific null matrix; a validated tombstone produces `validity=DECLARED_MISSING`, a null bundle hash, and null metric fields. Detected invalid evidence produces `validity=INVALID`, null metric fields, and the SHA-256 of the offending regular bytes when safely readable or null otherwise. A PRIMARY invalid row prevents PRIMARY scoring/bootstrap and permits only an artifact-state `INVALID`, scientific `INCONCLUSIVE`, no-authority diagnostic decision; a DESCRIPTIVE invalid row marks only its descriptive arm `INVALID` and is never read by the PRIMARY scorer.

`bootstrap.json` is an ordered array of PRIMARY contrast rows. Each row has exactly `stack_id,protocol_id,vector_id,alpha,m,resamples,seed_derivation,paired_seed_ids,lower_probability,upper_probability,lower_index,upper_index,estimate,lower,upper,width,precision`; `stack_id` must equal PRIMARY, `m` is byte-equal across rows and equals frozen family size, `paired_seed_ids` is sorted with length 31 or 32, all numeric values are finite, and `precision` is a strict boolean. It is absent only for the valid zero-survivor pilot decision; an eligible confirmation with no computable row is `INCONCLUSIVE`, not an empty successful bootstrap.

`decision.json` has exact top-level keys `schema_version,study_id,implementation_git_sha,execution_git_sha,lifecycle_state,artifact_state,scientific_result,blocker,primary_stack,descriptive_stack,g_roles,frozen_family,descriptive_arms,contrasts,core_gates,fault_guard,promotion_state,selected_protocols,runtime_authority,reasons`. `lifecycle_state` is `COMPLETE|STOPPED`; `artifact_state` is `VALID|INVALID` and describes only the PRIMARY decision surface, because each descriptive arm carries its own evidence state; `scientific_result` is `SUPPORTED|NOT_SUPPORTED|INCONCLUSIVE`; and `blocker` is null or `PROCESS_TIMEOUT|RESOURCE_CEILING|HOST_INTERRUPTION|MISSINGNESS|EVIDENCE_INVALID|CONTROL_FAILURE`. `primary_stack` has exact keys `stack_id,representation`; `descriptive_stack` is null or the same exact object. `g_roles` is byte-equal to the frozen stack-qualified array. `frozen_family` is byte-equal to `primary_family`. `descriptive_arms` is ordered by `(stack_id,protocol_id)` with exact keys `stack_id,protocol_id,selected_vector_id,reason,evidence_state`; the first four fields are byte-equal to frozen rows and `evidence_state` is `VALID|DECLARED_MISSING|INVALID|NOT_RUN`.

Each ordered PRIMARY `contrasts` row has exactly `stack_id,protocol_id,vector_id,n,estimate,lower,upper,width,precision,efficacy,core_gate_pass`. It is keyed by a frozen-family identity; `n` is 31 or 32; the four numeric values are finite; and the last three fields are strict booleans. Each ordered `core_gates` row has exactly `stack_id,protocol_id,gate_id,estimate_number,estimate_bool,threshold_number,comparison,pass`. `gate_id` is `UNSAFE_COUNT|JOINT_LIMIT_COUNT|QUEUE_OVERRUN_COUNT|TERMINAL_EMPTY|AGE_P95|HOLD_EXCESS|JERK_RATIO|DISCONTINUITY_RATIO`; `comparison` is `LE` except `TERMINAL_EMPTY`, which is `EQ_TRUE`. A numeric gate requires finite `estimate_number` and `threshold_number` and null `estimate_bool`; `TERMINAL_EMPTY` requires both numeric fields null, a strict boolean estimate, and `threshold_number=null`. `pass` is always a strict boolean. There is exactly one row per gate per contrast, ordered by frozen-family identity then the preceding gate order.

`fault_guard` is `PASS|FAIL|NOT_EVALUATED`; `promotion_state` is `PROMOTED|BLOCKED_BY_FAULT_GUARD|NO_SUPPORTED_PROTOCOL|INCONCLUSIVE`. `selected_protocols` and `runtime_authority` each have exact keys `deterministic_trajectory,flow_policy`. A selected deterministic protocol is one of `OPEN_LOOP|RECEDING_PREFIX|TEMPORAL_ENSEMBLE|LATEST_VALID|ASYNC_SAFE_PREFIX|OVERLAP_BLEND|NONE`; selected flow is `RTC_COMPATIBLE|NO_RUNTIME_AUTHORITY`. Runtime deterministic authority is one of `RECEDING_PREFIX|TEMPORAL_ENSEMBLE|LATEST_VALID|ASYNC_SAFE_PREFIX|OVERLAP_BLEND|NONE`, and runtime flow authority is `RTC_COMPATIBLE|NO_RUNTIME_AUTHORITY`; fallback OPEN_LOOP is never an authority value. Selection records the required evidence-based architectural choice; authority separately records what this run may publish for downstream execution. Each ordered `reasons` row has exactly `reason_id,detail`; `reason_id` is one of `NO_SURVIVORS,EFFICACY_PASS,EFFICACY_FAIL,IMPRECISE,CORE_GATE_FAIL,FAULT_GUARD_FAIL,FAULT_GUARD_NOT_EVALUATED,CONTROL_FAIL,MISSINGNESS_INVALID,EVIDENCE_INVALID,RESOURCE_STOP,FALLBACK_SELECTED,AUTHORITY_PROMOTED,AUTHORITY_WITHHELD,DESCRIPTIVE_ONLY`, and `detail` is a single NFC UTF-8 scalar of at most 256 code points with no CR/LF, NUL, C0/C1 control, bidi override, or unpaired surrogate.

The decision null/cardinality matrix is closed. A valid zero-survivor pilot decision is `COMPLETE/VALID/NOT_SUPPORTED`, blocker null, empty family/contrasts/gates, all descriptive arms `NOT_RUN`, `fault_guard=NOT_EVALUATED`, `promotion_state=NO_SUPPORTED_PROTOCOL`, `selected_protocols={deterministic_trajectory:OPEN_LOOP,flow_policy:NO_RUNTIME_AUTHORITY}`, `runtime_authority={deterministic_trajectory:NONE,flow_policy:NO_RUNTIME_AUTHORITY}`, and includes `FALLBACK_SELECTED`; this is a required baseline decision, not a novel-protocol promotion or confirmation-backed authority grant. A completed valid `NOT_SUPPORTED` confirmation with complete fault evidence uses the same selected fallback, empty runtime authority, `NO_SUPPORTED_PROTOCOL`, and `FALLBACK_SELECTED`. A completed confirmation with a nonempty family otherwise has exactly matching contrast/gate rows and blocker null. A valid PRIMARY fault tombstone preserves the already computed scientific selection, requires `fault_guard=NOT_EVALUATED`, promotion `INCONCLUSIVE`, and no authority; a supported result retains its supported winner/fallback selections, while a not-supported result retains the exact OPEN_LOOP fallback selection. A resource/missing/control stop is `STOPPED/VALID/INCONCLUSIVE` with its non-null blocker; corrupt evidence is `STOPPED/INVALID/INCONCLUSIVE` with `EVIDENCE_INVALID`; both have no selection or authority and include only fully computable contrast/gate rows. For any `COMPLETE/VALID/SUPPORTED` result, selected deterministic is the ranked supported B--F winner or `OPEN_LOOP` when no deterministic candidate supports, and selected flow is supported proven G or `NO_RUNTIME_AUTHORITY`; an OPEN_LOOP fallback requires `FALLBACK_SELECTED`. `PROMOTED` additionally requires blocker null and `fault_guard=PASS`; runtime deterministic authority is the selected B--F winner or `NONE` when selection is fallback OPEN_LOOP, runtime flow authority is the selected proven G or `NO_RUNTIME_AUTHORITY`, at least one novel authority field is populated, and at most one authority per field follows the role-specific tie rules. `BLOCKED_BY_FAULT_GUARD` requires `COMPLETE/VALID/SUPPORTED`, blocker null, `fault_guard=FAIL`, those same scientific selections, and no authority. `SUPPORTED` with `fault_guard=NOT_EVALUATED` likewise retains those scientific selections, uses promotion `INCONCLUSIVE`, and has no authority. `NO_SUPPORTED_PROTOCOL` requires `COMPLETE/VALID/NOT_SUPPORTED`, complete fault evidence, the exact OPEN_LOOP fallback selection, and empty authority; a valid fault-tombstoned NOT_SUPPORTED result instead uses promotion `INCONCLUSIVE` with that selection and empty authority. Every scientific `INCONCLUSIVE` uses promotion `INCONCLUSIVE` and no selection or authority. All schemas reject unknown/missing fields, duplicate identities, noncanonical order, bool-as-int, and nonfinite numeric values. Analyze scores only validated bundles and tombstones; it may safely classify invalid evidence but cannot load MuJoCo or modify a score.

The five required experiment documents are implementation-owned and have frozen LF-terminated heading templates. `README.md` has `# Experiment 02 — Temporal Action-Chunk Execution`, then `## Run`, `## Boundary`, `## Artifacts`; `CLAIM.md` has `# Experiment 02 Claim`, then exactly `## Hypothesis`, `## What this experiment can establish`, `## What this experiment cannot establish`, `## Independent variable`, `## Controlled variables`, `## Baselines`, `## Primary metric`, `## Secondary metrics`, `## Perturbation distribution`, `## Kill condition`, `## Advance condition`, `## Public source reused`, `## Public source studied but not imported`; `EXPERIMENT.md` has `# Experiment 02 Protocol`, then `## Preregistered protocol`, `## Schedule`, `## Metrics`, `## Gates`, `## Resources`, `## Reproduction`; `RESULTS.md` has a reviewed placeholder body and ends exactly with `## Claim tested`, `## Result`, the literal placeholder `INCONCLUSIVE — NOT YET RUN`, `## Evidence`, `## What we can claim`, `## What we cannot claim`, `## Interface implication`, `## Unitree R1 implication`, `## Next experiment justified by this result`, `## Experiments not justified by this result`; and `INTERFACE_FINDINGS.md` has `# Experiment 02 Interface Findings`, then `## Status`, the literal placeholder `NOT YET RUN`, `## Selected protocol`, `## Rejected protocols`, `## Reflect boundary`, `## Downstream implication`. Their exact placeholder hashes are committed before pilot. Report requires `README.md,CLAIM.md,EXPERIMENT.md` byte-identical to the implementation SHA and accepts `RESULTS.md,INTERFACE_FINDINGS.md` only as those exact placeholders or the byte-exact intended final render; no manual intermediate state is valid.

`experiments/02_action_chunks/ACTION_EXECUTION_DECISION.md` is a tracked, create-only rendering of `decision.json`, not an independent decision surface. Its exact LF-terminated template is heading `# Action Execution Decision`, then single-line fields in this order: `Study-ID`, `Implementation-SHA`, `Execution-SHA`, `Scientific-Result`, `Promotion-State`, `Deterministic-Trajectory-Protocol`, `Flow-Policy-Protocol`, `Deterministic-Runtime-Authority`, `Flow-Runtime-Authority`, `Primary-G-Role`, `Descriptive-G-Role`, `Fault-Guard`, `Decision-JSON-SHA256`, followed by `## Reasons`. The two protocol lines render `selected_protocols`; the two authority lines render `runtime_authority`, so `OPEN_LOOP` can be the required valid negative-result choice without being mislabeled as a promoted or confirmation-backed runtime authority. Each reason renders exactly as `- <reason_id>: <escaped-detail>`. Escaping first replaces backslash with two backslashes and then prefixes one backslash to each literal Markdown/HTML metacharacter in `` `*_{}[]()<>#+-!|& ``; the renderer accepts no raw CR/LF or control character and applies no HTML passthrough. The validator regenerates all three final Markdown files byte-for-byte from validated evidence before publication. A selected deterministic value must be one of `OPEN_LOOP,RECEDING_PREFIX,TEMPORAL_ENSEMBLE,LATEST_VALID,ASYNC_SAFE_PREFIX,OVERLAP_BLEND` when the scientific decision is complete and valid; flow may be `RTC_COMPATIBLE` only with frozen real-P3 provenance/conformance, and otherwise renders `NO_RUNTIME_AUTHORITY`. Stack-qualified approximation G renders as `DESCRIPTIVE_RTC_APPROXIMATION` for that role and can never yield `RTC_COMPATIBLE`; a missing descriptive stack renders `Descriptive-G-Role: NONE`. A `scientific_result=INCONCLUSIVE` or `lifecycle_state=STOPPED` decision renders `NONE` for deterministic selection and authority and `NO_RUNTIME_AUTHORITY` for flow selection and authority.

Report runs no physics and imports no MuJoCo. It revalidates the sealed analyze artifact, all prerequisite and Git identities including the P1 ledger/conformance probe and authentic P4 P3-gate chain, the exact placeholders/final bytes, the 1 MiB transaction cap, and a clean tree. Every rendered value is either a validated identifier/closed enum/numeric/hash or passes the same escaped-detail function; stderr, exception text, environment values, paths outside the closed inventories, and other raw strings are never rendered. Its marker `protocol/confirmation/report-manifest.json` has exactly `schema_version,study_id,implementation_git_sha,execution_git_sha,p1_provenance_hash,p4_provenance_hash,analyze_artifact_hash,decision_json_hash,files,total_bytes`; both provenance hashes are byte-equal to the validated frozen objects. `files` is the sorted exact three-row inventory for `RESULTS.md,INTERFACE_FINDINGS.md,ACTION_EXECUTION_DECISION.md`, with row keys `path,media_type,bytes,sha256`, and the marker excludes itself. The report transaction opens every destination parent descriptor-relative with no-follow, stages same-parent private files, writes canonical bytes, fsyncs every file and parent, accepts an existing destination only when it is the reviewed placeholder or byte-identical final, atomically replaces placeholders/renames absent outputs, and writes/fsyncs the marker last. This is create-only final publication: the sole permitted replacement is the committed reviewed placeholder, and a published final is never overwritten. Exact reissue validates and skips. A crash before the marker leaves no publishable report: same-process cleanup removes only continuously held inodes, while restart descriptor-safely moves each verified orphan to bounded quarantine and never deletes it. A conflicting final, unsafe orphan, partially final set with nonmatching bytes, post-write Git/source drift, or marker mismatch fails closed and cannot be committed. The orchestrator commits exactly the three Markdown files and marker, then requires a completely clean status; runtime authority exists only at that clean report commit.

Missing execution and invalid evidence are disjoint. A declared episode that cannot finish solely because of `PROCESS_TIMEOUT`, `RESOURCE_CEILING`, or `HOST_INTERRUPTION` may publish one create-only canonical line in `missing-tombstones.jsonl` with exact keys `schema_version,study_id,phase,stack_id,protocol_id,vector_id,seed,cell_id,shard_id,attempt_id,reason,started_at_utc,ended_at_utc,command_sha256,stderr_sha256`. Times are canonical UTC, finish is not before start, IDs match the frozen iterator, and an identity cannot have both a bundle and tombstone; absent stderr is the SHA-256 of empty bytes. The file is present and canonically empty when there are no tombstones, is sealed by the stage artifact manifest, and uses the same descriptor-relative create-only/fsync publication rule as bundles. A tombstone is `DECLARED_MISSING`, never a zero. One PRIMARY candidate **core** tombstone invalidates that candidate seed value and excludes its otherwise valid paired PRIMARY A seed only for that contrast; one such paired-seed exclusion per contrast is allowed, leaving at least 31 pairs. A PRIMARY A core tombstone excludes that A seed from every contrast and counts once against each. The allowance is exactly one tombstoned core cell for the affected `(stack,protocol,seed)`; a second core tombstone with the same stack/protocol in any seed/cell is mechanically `systematic protocol-specific loss`. A second excluded paired seed, systematic loss, or any missing PRIMARY A control/stationary control is `INCONCLUSIVE`. A tombstone in any PRIMARY fault probe never consumes the core allowance: it leaves an otherwise valid core scientific label reportable, sets `fault_guard=NOT_EVALUATED`, forces `promotion_state=INCONCLUSIVE`, and yields no runtime authority. A DESCRIPTIVE core, control, or fault tombstone invalidates only that descriptive arm and neither removes a PRIMARY pair nor changes the PRIMARY scientific label/guard.

A malformed, noncanonical, corrupt, hash-mismatched, symlinked, duplicate, conflicting, or validator-rejected bundle/tombstone is `INVALID`, never `DECLARED_MISSING`; one such identity makes its shard invalid. A PRIMARY invalid shard makes the PRIMARY study `INCONCLUSIVE`; a DESCRIPTIVE invalid shard makes only that arm `INVALID`. Shard completeness means every frozen identity resolves to exactly one validated bundle or validated tombstone and its shard row has the matching `COMPLETE|DECLARED_MISSING` disposition. `INVALID` is a terminal diagnostic disposition and cannot satisfy completeness. Resume may validate-and-skip a complete bundle or tombstone but never rewrites either.

Publication creates a private same-parent temporary bundle while holding its directory descriptor, writes canonical rollout and sidecars, fsyncs files, writes and fsyncs the manifest last, fsyncs the temporary directory, atomically renames to an absent final identity, then fsyncs the parent. Same-process failure cleanup may remove only the temporary tree whose descriptor/inode has been held continuously since creation.

After restart, no process can claim held-inode continuity. Recovery opens the parent and exact `.<identity>.partial-<nonce>` entries descriptor-relatively with no-follow, rejects symlinks/non-directories/foreign owner or identity/malformed or ambiguous entries, and atomically renames each single verified orphan to an absent descriptor-relative `quarantine/<identity>.<nonce>`. Restart recovery never deletes or publishes an orphan. Quarantine remains evidence-bearing, counts against the byte ceiling, and requires explicit later review. Any unsafe or ambiguous recovery state fails closed. Resume skips only an exact complete final bundle; partial/conflicting output fails. `--max-episodes` is smoke-only unless it equals the exact shard count.

## Decision and implementation boundary

For each displacement, recovery success is `1` only when the unchanged P4 success condition—EEF error at most `0.025 m` continuously for `0.10 s`—first completes within the displacement's `2.0 s` window; otherwise it is `0`, including a valid censored non-recovery. A one-move episode score is its one indicator and a two-move episode score is the arithmetic mean of its two indicators. For one `(protocol,stack,seed)`, the primary value is the equal-weight arithmetic mean of the eight complete no-fault core episode scores. The paired seed contrast is `candidate-A`, so positive is beneficial. The minimum worthwhile effect is fixed before pilot at `delta=0.05` (five percentage points); it is not pilot-selected.

For each frozen candidate contrast, sort the complete paired seed IDs ascending. `frozen_manifest_sha256` is exactly the lowercase SHA-256 of the canonical bytes of `experiments/02_action_chunks/configs/frozen.yaml` recorded by the confirmation protocol manifest. PCG64 uses the first 128 big-endian bits of SHA-256 over canonical JSON `[frozen_manifest_sha256,"exp02-bootstrap-v1",stack_id,protocol_id]`. Each of 10,000 resamples draws the complete paired-seed count with replacement and stores the mean paired contrast. Sort values ascending; for probability `p`, use index `max(0,ceil(p*10000)-1)` with no interpolation or tie deduplication. Lower/upper probabilities are the Bonferroni values above. Efficacy requires the adjusted lower endpoint strictly greater than `0.05`; equality fails efficacy. A conclusive exclusion of the worthwhile effect requires the adjusted upper endpoint strictly less than `0.05`; equality or an interval spanning `0.05` is imprecise.

One validated `DECLARED_MISSING` PRIMARY core tombstone invalidates that `(candidate,seed)` value and excludes its paired A seed only for that contrast as specified above. At least 31 of 32 paired seeds and adjusted interval width at most `0.20` are required for a precise contrast. A second missing paired seed for one contrast, systematic PRIMARY protocol-specific loss, any missing PRIMARY A control/stationary control, or any PRIMARY `INVALID` evidence/shard makes the PRIMARY study `INCONCLUSIVE`. Corruption is always `INVALID` and is never eligible for the one-missing allowance. Censored non-recovery is observed zero, never missing. No value is imputed. A frozen-family candidate with complete valid evidence but an observed core absolute-gate failure remains in `m`, fails that gate, and cannot be silently removed or used to reduce multiplicity; a lifecycle/evidence invariant failure is instead invalid evidence and therefore `INCONCLUSIVE`.

Core absolute confirmation gates are exact. Every authority-eligible candidate requires on the eight core cells: zero nonfinite or unclamped unsafe output; zero hard joint-limit violation; zero queue overrun; terminal empty queues; p95 action age at most `1.00 s`; pooled hold-tick fraction no greater than the paired A fraction plus `0.05`; and p95 joint jerk and p95 command discontinuity each at most `1.5` times the valid P5 pilot-validation A value for the identical promoted P4 stack. Episode percentiles use the P4 nearest-rank rule; jerk/discontinuity first compute episode p95, then nearest-rank p95 across exactly `8*n` complete core episodes with equal episode weight, where paired `n` is 32 or 31 after the sole allowed exclusion. The candidate and A use the identical frozen paired-seed set for every pooled gate. Action age pools stored 100 Hz valid executed-reference samples over those same episodes; hold ticks have no proposal age and are excluded from that domain. Equality to an absolute maximum passes. These are the `core_gates` used by `scientific_result`.

Fault probes have a separate promotion guard over the complete PRIMARY fault inventory. A PRIMARY fault-probe lifecycle/evidence invariant failure, including incorrect pause generation/coverage, is invalid evidence and makes both scientific result and promotion `INCONCLUSIVE`. A validated PRIMARY fault tombstone instead preserves the independently computed core scientific label and its corresponding selected protocol/fallback, but sets `fault_guard=NOT_EVALUATED`, `promotion_state=INCONCLUSIVE`, and no runtime authority. With complete valid PRIMARY fault evidence, any nonfinite/unclamped unsafe output, hard joint-limit violation, queue overrun, unresolved request/queue, or failure to enter safe hold when required sets `fault_guard=FAIL` but does not change the already computed scientific label or selection. It yields `promotion_state=BLOCKED_BY_FAULT_GUARD` only when the scientific result is `SUPPORTED`; a `NOT_SUPPORTED` result remains `NO_SUPPORTED_PROTOCOL` because there is no novel protocol to block, while the failed guard remains explicit and authority is empty. Otherwise the guard is `PASS`. Every declared PRIMARY stationary control is part of scientific validity and must have the exact zero-move metric shape, `stationary_error_n=2375`, finite maximum EEF error at most `0.025 m`, `recovery_event_count=0`, and `stationary_control_pass=true`; failure is `CONTROL_FAIL` and makes the study `INCONCLUSIVE`, never a candidate loss. Descriptive control/fault failures are rendered only with their arm. This separation is explicit in both decision artifacts; a scientifically supported protocol with failed or unevaluated fault guard has no runtime authority.

Within supported deterministic-trajectory candidates, rank the adjusted lower endpoint descending and use fixed `B,C,D,E,F` order only for exact numerical ties; proven compatible G is the sole flow-policy candidate and is evaluated separately. `SUPPORTED` means at least one frozen-family candidate clears efficacy and every core gate. `NOT_SUPPORTED` means zero authority-eligible pilot survivors after valid A with no efficacy claim, or every completed valid frozen-family candidate has adjusted upper endpoint strictly below `0.05` or fails a predeclared core non-efficacy gate while all A/control, missingness, and precision requirements remain valid. `INCONCLUSIVE` applies after eligible execution begins when no candidate supports and at least one contrast equals/spans `0.05`, lacks precision, has disallowed missingness, or A/control/evidence/shard validity fails. Descriptive-stack results, approximation-only G, and valid fault outcomes never affect the PRIMARY scientific label. The first ranked supported B--F becomes the deterministic scientific winner; if none supports in a complete valid decision, deterministic selection falls back to OPEN_LOOP. A supported proven G becomes the flow scientific winner independently. Only supported non-anchor winners can receive runtime authority, so at most one value occupies each authority field and both may be populated.

Promotion is evaluated only after the scientific label. `SUPPORTED + fault_guard=PASS` yields `PROMOTED` and publishes every role-specific scientific winner in selection and authority; the exact deterministic mapping is `B=RECEDING_PREFIX,C=TEMPORAL_ENSEMBLE,D=LATEST_VALID,E=ASYNC_SAFE_PREFIX,F=OVERLAP_BLEND`, while supported proven G maps only to flow `RTC_COMPATIBLE`. When only G supports, deterministic selection is the valid A/`OPEN_LOOP` fallback but deterministic runtime authority remains `NONE`; G supplies the required novel authority. `SUPPORTED + fault_guard=FAIL` yields `BLOCKED_BY_FAULT_GUARD`: the supported winners and any required deterministic OPEN_LOOP fallback remain explicit selections, but authority is empty. `SUPPORTED + fault_guard=NOT_EVALUATED` yields `INCONCLUSIVE` promotion with those selections retained, no authority, and `scientific_result=SUPPORTED`. Complete valid `NOT_SUPPORTED` yields `NO_SUPPORTED_PROTOCOL`, selects deterministic `OPEN_LOOP` as the required baseline fallback, selects no flow protocol, grants no runtime authority, and records that this is neither promotion nor a confirmation-backed authority grant. Scientific `INCONCLUSIVE` yields promotion `INCONCLUSIVE`, no selection, and no authority. Deterministic and flow selection/authority fields may differ only as these closed rules permit, and none may name `RTC_APPROXIMATION`. Raw descriptive winners are reported in reasons and never gain runtime authority.

`experiments/02_action_chunks` owns only its five required documents, decision Markdown, configs/run/broker/protocol/schedule/adapter/evaluate/tests/bundles/reports. It imports no study-only ACT/LeRobot/OpenPI code and adds no experiment framework to `reflect/`. Parallel work has disjoint broker/protocol, scheduler/adapter/evaluator, and config/CLI/artifact ownership; integration is broker then adapter then replay; live shards are serial. Tests cover formulas/counts and the 7502 MiB/50-wall-hour/124-CPU-hour caps; the 450-row/10,800-float-byte/65,536-Parquet-byte policy-raw bounds and 6,250-row/150,000-float-byte/262,144-Parquet-byte normalized-proposal bounds; exact nine-knot-to-125-row interpolation, pre/post-fault reconstruction, and C/F/G replay; stack-qualified selections, PRIMARY multiplicity, and DESCRIPTIVE isolation; B prefix versus E lead units and every E continuity class; the alternative conflict proof, exact C contributor set, F/G `h>=1`, and explicit A/B/E-v0 overlap N/A/handoff states; exact alternative/discontinuity bytes and paused actual-delivery validity; the exact recurrence and tick order; both full dwell windows without changing the eight cells; tick-0 canonical lifecycle; request/move chronology; inflight equality; lifecycle and pause/drop distinction; immutable issued ticks; eligibility and stack-qualified G roles; terminal cutoff and empty queues; hold latch/zero-dq/no-DIK/settling; every secondary metric domain/null rule; zero-move stationary schema, 2,375-sample error domain, and recovery-event control failure; core/fault/descriptive `DECLARED_MISSING` matrices versus corrupt/`INVALID`; prerequisite `NOT_RUN` versus scientific `INCONCLUSIVE`; atomic crash/resume/quarantine and their caps; paired equality; scientific-result/fault-guard/promotion/selection/authority separation, including zero-survivor, completed-negative, G-only, fault-pass, fault-fail, fault-tombstone, valid `NOT_SUPPORTED -> OPEN_LOOP` fallback, and no-authority render matrices; closed bootstrap/decision/gate schemas; exact three-Markdown regeneration and hostile Markdown/HTML reason details; stage inventories/seed hash null matrix/freeze/report commit cleanliness; `--dry-run` zero-write/no-import and `--seed` assertion; authentic P4 full-lifecycle acceptance plus missing/extra/symlink/content/Git-blob mutations of `configs/p3-gate.yaml` and every other hash-chain/allowlist link; all seven P1 module/content/API mutations, including `_rollout_io.py`, at every stage boundary; headless replay; source/P4 hashes; safety; secret/artifact scan; and diff check.

## Executable schedule self-review

The schedule implementation must expose a pure iterator used by both runner and tests. Golden hand examples are executable assertions, not prose-only examples:

- Core, all A--G, `ell=25`: setup accepts at 0; `r0` requests at 50; `m1=51` occurs while r0 is outstanding; normal r0 delivers at 75; the first recovery window ends at 1051; `m2=1052`; the second recovery window ends at 2052. The two-move cell remains one episode and both displacement indicators remain in its arithmetic-mean score.
- Periodic, `I=1,ell=75`: desired epochs are 50,100,150; actual `s0=50,d0=125`; the epoch 100 is capped through request phase 125 and admits at `s1=126,d1=201`; the epoch 150 admits at `s2=202`, not 201, because delivery follows request processing.
- Periodic, `I=2,ell=150`: actual `s0=50,s1=100`; the desired epoch 150 is capped, `d0=200`, and the next request admits at 201; outstanding count is never above two.
- E prefetch: after an r0 delivery at 200, v0 requests at 300 and with `ell=25` delivers at expiry 325 (`EXPIRY_HANDOFF`, zero hold); v1 requests at 250 and delivers at 275 (`STRICT_OVERLAP`); v2 with `ell=150` requests at 201, delivers at 351, and has exactly 26 `UNAVOIDABLE_HOLD` ticks after expiry 325.
- Alternative conflict: at `ell=25`, r0 covers `[75,200)`; periodic r1 requests at 76 and arrives at 101, so C has exactly two contributors and F/G have 99 available overlap ticks with `h>=1`. E-v0 arrives at 200 (`EXPIRY_HANDOFF`), E-v1 at 150 (50 overlap ticks), and E-v2 at 101 (99 overlap ticks); A and B never claim overlap reconciliation.
- Old-after-newer: `s0/d0=50/200`, `s1=201`, `s2=251`, `d2=401`, `d1=402`; r2 accepts and r1 is lifecycle-addressably rejected.
- Worst terminal pause: request at 2500, normal ell-350 delivery 2850, paused delivery 3000, executable expiry 3125; no request is created at 2501 and terminal queues are empty.

This review-fix revision additionally freezes the executable statistical decision rule; the authentic post-implementation P4 P3-gate chain; the minimal P1 module/API ledger; byte-preserved nine-knot policy-raw to normalized-to-derived C/F/G ownership and recomputation; a positive-overlap conflict schedule; per-stack tuning and PRIMARY/DESCRIPTIVE authority; meaningful asynchronous prefetch; exact fault payloads; prerequisite, missingness, and decision null matrices; warm-up invalidity boundary; representation-dispatch safe hold; and the root-relative staged CLI plus strict evidence schemas. It preserves one common exogenous core schedule, exact capacity-aware recurrence, every declared cell/count/resource maximum, feasible out-of-order delivery, terminal closure, whole-study anchor semantics, fixed survivor multiplicity, evidence-bearing restart recovery, and maxima derived from one count table. The defined G projection remains an approximation; only a distinct provenance-bound path can claim `RTC_COMPATIBLE`.
